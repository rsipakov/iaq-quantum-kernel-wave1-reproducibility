# Archived Legacy Preprocessing

The preprocessing code in this directory is retained only as historical source-context provenance.

It is not part of the supported Wave 1 artifact-level reproduction path. The supported Wave 1 analysis consumes the frozen prepared subset, statevector reference, raw hardware results, reconstructed kernels, and analysis artifacts already included in the repository.

